// Generated by CoffeeScript 2.1.0
(function() {
  var Page, ReviewDesktopPage;

  Page = require('./Page');

  module.exports = ReviewDesktopPage = class ReviewDesktopPage extends Page {
    constructor({reviewId}) {
      super({
        url: `http://www.amazon.com/review/${reviewId}`
      });
      this.reviewId = reviewId;
    }

    parse() {
      var profileTag, profileUrl, profileUrlComponents;
      profileTag = this.$('a.author');
      profileUrl = profileTag.attr('href') || '';
      profileUrlComponents = (profileUrl != null ? profileUrl.split('/') : void 0) || [];
      return {
        profile: {
          name: profileTag.text(),
          url: profileUrl,
          id: (profileUrlComponents.length >= 4 && profileUrlComponents[3]) || ''
        }
      };
    }

  };

}).call(this);
